<?xml version="1.0" encoding="utf-8"?>
<!--
  NFR-SEC-03: the database lives in app-private internal storage and never
  leaves the device except by explicit user export. allowBackup is false in the
  manifest; these rules make the same statement to the API 31+ backup agents so
  neither cloud backup nor device-to-device transfer can copy the ledger.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
